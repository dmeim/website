---
import BaseLayout from "../../layouts/BaseLayout.astro";
import PageIntro from "../../components/react/PageIntro";
import ToolsShell from "../../components/tools/ToolsShell.astro";
import WordCodeGenerator from "../../components/tools/islands/WordCodeGenerator";
import QRCodeGenerator from "../../components/tools/islands/QRCodeGenerator";
import SvgPlaceholderGenerator from "../../components/tools/islands/SvgPlaceholderGenerator";
import CameraRecorder from "../../components/tools/islands/CameraRecorder";
import TokenGenerator from "../../components/tools/islands/TokenGenerator";
import HashText from "../../components/tools/islands/HashText";
import Bcrypt from "../../components/tools/islands/Bcrypt";
import UuidGenerator from "../../components/tools/islands/UuidGenerator";
import UlidGenerator from "../../components/tools/islands/UlidGenerator";
import PasswordStrengthAnalyser from "../../components/tools/islands/PasswordStrengthAnalyser";
import HmacGenerator from "../../components/tools/islands/HmacGenerator";
import Encryption from "../../components/tools/islands/Encryption";
import Bip39Generator from "../../components/tools/islands/Bip39Generator";
import RsaKeyPairGenerator from "../../components/tools/islands/RsaKeyPairGenerator";
import PdfSignatureChecker from "../../components/tools/islands/PdfSignatureChecker";
import TextToNatoAlphabet from "../../components/tools/islands/TextToNatoAlphabet";
import TextToBinary from "../../components/tools/islands/TextToBinary";
import TextToUnicode from "../../components/tools/islands/TextToUnicode";
import RomanNumeralConverter from "../../components/tools/islands/RomanNumeralConverter";
import BaseConverter from "../../components/tools/islands/BaseConverter";
import ListConverter from "../../components/tools/islands/ListConverter";
import Base64StringConverter from "../../components/tools/islands/Base64StringConverter";
import Base64FileConverter from "../../components/tools/islands/Base64FileConverter";
import { availableTools, type Tool } from "../../content/tools";

export function getStaticPaths() {
  return availableTools().map((tool) => ({
    params: { slug: tool.id },
    props: { tool },
  }));
}

interface Props {
  tool: Tool;
}

const { tool } = Astro.props;
const slug = tool.id;
---

<BaseLayout title={tool.title} description={tool.description}>
  <section class="tools-page" aria-labelledby="tool-heading">
    <PageIntro
      client:visible
      title={tool.title}
      titleId="tool-heading"
      lede={tool.description}
    />

    <ToolsShell activeSlug={slug}>
      <div class="tool-stage">
        <p class="tool-stage__eyebrow">Tool</p>

        {/* Island mounts — one React island per available tool slug. */}
        {
          slug === "word-code-generator" ? (
            <div class="tool-mount" data-tool={slug}>
              <WordCodeGenerator client:load />
            </div>
          ) : slug === "qrcode-generator" ? (
            <div class="tool-mount" data-tool={slug}>
              <QRCodeGenerator client:load />
            </div>
          ) : slug === "svg-placeholder-generator" ? (
            <div class="tool-mount" data-tool={slug}>
              <SvgPlaceholderGenerator client:load />
            </div>
          ) : slug === "camera-recorder" ? (
            <div class="tool-mount" data-tool={slug}>
              <CameraRecorder client:load />
            </div>
          ) : slug === "token-generator" ? (
            <div class="tool-mount" data-tool={slug}>
              <TokenGenerator client:load />
            </div>
          ) : slug === "hash-text" ? (
            <div class="tool-mount" data-tool={slug}>
              <HashText client:load />
            </div>
          ) : slug === "bcrypt" ? (
            <div class="tool-mount" data-tool={slug}>
              <Bcrypt client:load />
            </div>
          ) : slug === "uuid-generator" ? (
            <div class="tool-mount" data-tool={slug}>
              <UuidGenerator client:load />
            </div>
          ) : slug === "ulid-generator" ? (
            <div class="tool-mount" data-tool={slug}>
              <UlidGenerator client:load />
            </div>
          ) : slug === "password-strength-analyser" ? (
            <div class="tool-mount" data-tool={slug}>
              <PasswordStrengthAnalyser client:load />
            </div>
          ) : slug === "hmac-generator" ? (
            <div class="tool-mount" data-tool={slug}>
              <HmacGenerator client:load />
            </div>
          ) : slug === "encryption" ? (
            <div class="tool-mount" data-tool={slug}>
              <Encryption client:load />
            </div>
          ) : slug === "bip39-generator" ? (
            <div class="tool-mount" data-tool={slug}>
              <Bip39Generator client:load />
            </div>
          ) : slug === "rsa-key-pair-generator" ? (
            <div class="tool-mount" data-tool={slug}>
              <RsaKeyPairGenerator client:load />
            </div>
          ) : slug === "pdf-signature-checker" ? (
            <div class="tool-mount" data-tool={slug}>
              {/* client:only — pdf-signature-reader / node-forge must not enter Worker SSR */}
              <PdfSignatureChecker client:only="react" />
            </div>
          ) : slug === "text-to-nato-alphabet" ? (
            <div class="tool-mount" data-tool={slug}>
              <TextToNatoAlphabet client:load />
            </div>
          ) : slug === "text-to-binary" ? (
            <div class="tool-mount" data-tool={slug}>
              <TextToBinary client:load />
            </div>
          ) : slug === "text-to-unicode" ? (
            <div class="tool-mount" data-tool={slug}>
              <TextToUnicode client:load />
            </div>
          ) : slug === "roman-numeral-converter" ? (
            <div class="tool-mount" data-tool={slug}>
              <RomanNumeralConverter client:load />
            </div>
          ) : slug === "base-converter" ? (
            <div class="tool-mount" data-tool={slug}>
              <BaseConverter client:load />
            </div>
          ) : slug === "list-converter" ? (
            <div class="tool-mount" data-tool={slug}>
              <ListConverter client:load />
            </div>
          ) : slug === "base64-string-converter" ? (
            <div class="tool-mount" data-tool={slug}>
              <Base64StringConverter client:load />
            </div>
          ) : slug === "base64-file-converter" ? (
            <div class="tool-mount" data-tool={slug}>
              <Base64FileConverter client:load />
            </div>
          ) : (
            <div class="tool-mount" data-tool={slug}>
              <p class="tool-mount__note">Unknown tool.</p>
            </div>
          )
        }
      </div>
    </ToolsShell>
  </section>
</BaseLayout>

<style>
  .tools-page {
    display: grid;
    gap: var(--space-2xl);
  }

  .tools-page :global(.page-intro) {
    margin-bottom: 0;
  }

  /* Named tool-stage (not tool-workspace) to avoid clashing with shared island CSS */
  .tool-stage {
    display: grid;
    gap: var(--space-lg);
  }

  .tool-stage__eyebrow {
    margin: 0;
    font-size: 0.6875rem;
    font-weight: 500;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--color-gold);
  }

  .tool-mount {
    display: grid;
    gap: var(--space-md);
    min-height: 12rem;
    padding-top: var(--space-sm);
    border-top: 1px solid var(--color-border);
  }

  .tool-mount__note {
    margin: 0;
    max-width: 48ch;
    color: var(--color-stone);
    font-size: 0.9375rem;
    line-height: 1.6;
  }

  .tool-mount__note code {
    font-size: 0.9em;
    color: var(--color-ink);
  }
</style>
